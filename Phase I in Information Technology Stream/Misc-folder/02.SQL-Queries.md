
---

# ✅ **25 Easy SQL MCQs**

### **1. Which SQL command is used to remove all rows from a table but keep its structure?**

A. DELETE
B. DROP
C. TRUNCATE
D. REMOVE

---

### **2. Which SQL statement is used to extract data from a database?**

A. GET
B. SELECT
C. EXTRACT
D. FETCH

---

### **3. What does the ALTER command do?**

A. Deletes a table
B. Changes the structure of a table
C. Inserts rows
D. Displays data

---

### **4. Which of the following is NOT an aggregate function?**

A. COUNT
B. SUM
C. AVG
D. WHERE

---

### **5. Which JOIN returns only matching rows from both tables?**

A. LEFT JOIN
B. RIGHT JOIN
C. FULL JOIN
D. INNER JOIN

---

### **6. Which clause filters rows after the SELECT statement?**

A. FROM
B. HAVING
C. WHERE
D. ORDER BY

---

### **7. Which clause is used to filter groups created using GROUP BY?**

A. WHERE
B. HAVING
C. FILTER
D. ORDER BY

---

### **8. UNION operator returns:**

A. Only common rows
B. All rows from both queries (removing duplicates)
C. All rows including duplicates
D. Only unique rows from first query

---

### **9. INTERSECT operator returns:**

A. Rows from both tables including duplicates
B. Rows common to both queries
C. Rows not common to both queries
D. All rows sorted

---

### **10. EXCEPT operator returns:**

A. Rows common in both queries
B. Rows in the second query only
C. Rows in the first query but not in the second
D. All rows from both queries

---

### **11. Which command permanently deletes a table from the database?**

A. TRUNCATE
B. DELETE
C. DROP
D. ALTER

---

### **12. DELETE command removes rows based on:**

A. Primary key
B. Constraints
C. WHERE clause
D. ORDER BY clause

---

### **13. Which SQL clause checks if a value belongs to a list?**

A. BETWEEN
B. LIKE
C. IN
D. EXISTS

---

### **14. EXISTS clause returns TRUE if:**

A. A column has NULL values
B. A subquery returns at least one row
C. A value matches a pattern
D. A table exists

---

### **15. Nested queries are also known as:**

A. Outer queries
B. Subqueries
C. Inline views
D. All of the above

---

### **16. A VIEW in SQL is:**

A. A physical table
B. A virtual table
C. A stored procedure
D. A backup table

---

### **17. Which JOIN returns all records from left table and matching records from right?**

A. RIGHT JOIN
B. LEFT JOIN
C. FULL JOIN
D. INNER JOIN

---

### **18. Which operator allows combining results of two SELECT queries including duplicates?**

A. UNION
B. UNION ALL
C. INTERSECT
D. EXCEPT

---

### **19. What does SELECT COUNT(*) return?**

A. Number of non-null values
B. Number of rows including duplicates
C. Number of unique rows
D. Number of columns

---

### **20. To update values in a table we use:**

A. CHANGE
B. ALTER
C. MODIFY
D. UPDATE

---

### **21. Which SQL clause sorts a result set?**

A. GROUP BY
B. ORDER BY
C. WHERE
D. HAVING

---

### **22. FULL OUTER JOIN returns:**

A. Only matching rows
B. Only non-matching rows
C. All rows from both tables
D. Only unique rows

---

### **23. SELECT DISTINCT is used to:**

A. Delete duplicate rows
B. Display only unique rows
C. Update unique rows
D. Join tables

---

### **24. The wildcard character for matching multiple characters in LIKE is:**

A. ?
B. _
C. %
D. *

---

### **25. A subquery used in FROM clause is called:**

A. Inline view
B. Nested view
C. Inline table
D. Temporary table

---

# ✅ **Answer Key**

1–C
2–B
3–B
4–D
5–D
6–C
7–B
8–B
9–B
10–C
11–C
12–C
13–C
14–B
15–D
16–B
17–B
18–B
19–B
20–D
21–B
22–C
23–B
24–C
25–A

---

---

# ✅ **40 Medium-Level SQL MCQs**

### **1. Which statement about TRUNCATE is TRUE?**

A. It can be rolled back
B. It fires triggers
C. It removes rows faster than DELETE
D. It works only when WHERE clause is provided

---

### **2. Which of the following cannot be used with aggregate functions?**

A. GROUP BY
B. HAVING
C. DISTINCT
D. WHERE

---

### **3. In an INNER JOIN between Table A and Table B, if A has 5 rows and B has 3 matching rows, the result will contain:**

A. 5 rows
B. 3 rows
C. 8 rows
D. Depends on the join column

---

### **4. Which join returns rows only when there is NO match between tables?**

A. LEFT JOIN
B. RIGHT JOIN
C. FULL OUTER JOIN
D. None of the above

---

### **5. Which statement about VIEW is correct?**

A. It stores data permanently
B. It is a virtual table
C. It cannot contain joins
D. It must contain a primary key

---

### **6. UNION ALL differs from UNION because it:**

A. Removes duplicates
B. Sorts automatically
C. Keeps duplicates
D. Can only combine identical tables

---

### **7. Which keyword restricts a subquery to return only one row?**

A. LIMIT 1
B. DISTINCT
C. ANY
D. IN

---

### **8. Which clause is evaluated first in a SELECT statement?**

A. ORDER BY
B. WHERE
C. SELECT
D. FROM

---

### **9. Which operator checks multiple conditions and returns TRUE if any condition matches?**

A. ALL
B. ANY
C. EXISTS
D. BETWEEN

---

### **10. Which statement will rename a column in a table?**

A. ALTER TABLE t RENAME c1 TO c2
B. UPDATE t SET c1 = c2
C. MODIFY TABLE t RENAME COLUMN c1 c2
D. ALTER TABLE t CHANGE c1 c2

---

### **11. Which aggregate function ignores NULL values?**

A. COUNT(*)
B. COUNT(column)
C. SUM(*)
D. ALL aggregate functions include NULLs

---

### **12. Which JOIN produces the maximum number of rows?**

A. INNER JOIN
B. LEFT JOIN
C. RIGHT JOIN
D. FULL OUTER JOIN

---

### **13. What does the following query return?

`SELECT MAX(salary) FROM employees GROUP BY department;`**
A. Maximum salary from each department
B. Maximum salary from the company
C. Total number of departments
D. Minimum salary from each department

---

### **14. Which SQL keyword is used to compare a value with a list returned by a subquery?**

A. IN
B. LIKE
C. EXISTS
D. DISTINCT

---

### **15. DELETE without WHERE clause removes:**

A. Only NULL rows
B. Only duplicate rows
C. All rows
D. First 100 rows

---

### **16. Which of the following is TRUE for EXISTS?**

A. It checks values in the outer query
B. It checks whether subquery returns rows
C. It checks equality between two columns
D. It requires GROUP BY

---

### **17. A correlated subquery is executed:**

A. Once for the entire query
B. Only when called
C. Once for every row of outer query
D. Only at the end

---

### **18. FULL OUTER JOIN returns:**

A. Only matching rows
B. Only non-matching rows
C. Matching + non-matching rows
D. Only NULL rows

---

### **19. What is the result of

`SELECT COUNT(DISTINCT salary) FROM emp;`**
A. Number of salary values including duplicates
B. Number of unique salary values
C. Number of employees with salary > distinct
D. Count of all employees

---

### **20. Which clause must appear after GROUP BY?**

A. WHERE
B. ORDER BY
C. HAVING
D. DISTINCT

---

### **21. INTERSECT returns rows that are:**

A. Unique
B. Common to both queries
C. Not present in second query
D. All rows from both queries

---

### **22. Which is faster for deleting all rows?**

A. DELETE
B. DELETE WHERE 1=1
C. TRUNCATE
D. DROP

---

### **23. Which is NOT allowed inside a view?**

A. SELECT with JOIN
B. SELECT using WHERE
C. SELECT using aggregate functions
D. TRUNCATE statement

---

### **24. To find employees earning more than the average salary, use:**

A. Subquery
B. JOIN
C. UNION
D. INTERSECT

---

### **25. What does this return?

`SELECT AVG(salary) OVER() FROM emp;`**
A. Average salary per department
B. Total employees
C. Overall average salary
D. Average based on GROUP BY

---

### **26. The default behavior of UNION is:**

A. Include duplicates
B. Exclude duplicates
C. Perform a full join
D. Sort descending

---

### **27. Which operator is used to compare a value with ALL values in a list?**

A. ANY
B. IN
C. LIKE
D. ALL

---

### **28. Which query finds rows in Table A that do NOT exist in Table B?**

A. A INNER JOIN B
B. A LEFT JOIN B WHERE B.id IS NULL
C. A RIGHT JOIN B WHERE B.id IS NULL
D. A FULL JOIN B WHERE A.id IS NOT NULL

---

### **29. What is TRUE about INNER JOIN with ON condition?**

A. Returns unmatched rows
B. Returns only rows satisfying join condition
C. Ignores NULLs and returns all rows
D. Same as CROSS JOIN

---

### **30. Which of the following is TRUE for CROSS JOIN?**

A. Requires join condition
B. Produces Cartesian product
C. Same as FULL JOIN
D. Removes duplicates

---

### **31. Which is used to update a table structure?**

A. UPDATE
B. ALTER
C. MODIFY
D. CHANGE

---

### **32. Which join returns all rows from right table and matched from left?**

A. LEFT JOIN
B. FULL JOIN
C. INNER JOIN
D. RIGHT JOIN

---

### **33. IN operator internally behaves like:**

A. Multiple OR conditions
B. AND condition
C. EXISTS
D. DISTINCT

---

### **34. Which subquery type returns more than one row?**

A. Scalar subquery
B. Row subquery
C. Multiple-column subquery
D. Table subquery

---

### **35. Which of the following is NOT a valid set operator?**

A. UNION
B. JOIN
C. INTERSECT
D. EXCEPT

---

### **36. What does this return?

`SELECT department, COUNT(*) FROM emp GROUP BY department HAVING COUNT(*) > 5;`**
A. All departments
B. Departments with >5 employees
C. Departments with NULL values
D. Departments with highest salaries

---

### **37. A view created with CHECK OPTION ensures:**

A. Cannot update view
B. Updates must satisfy view condition
C. View is read-only
D. View always shows full table

---

### **38. INSERT INTO view works only if:**

A. View is based on a single base table
B. View contains GROUP BY
C. View uses DISTINCT
D. View contains JOINs

---

### **39. Which SQL clause removes duplicate rows in a result set?**

A. UNIQUE
B. DISTINCT
C. CLEAN
D. HAVING

---

### **40. ALTER TABLE ADD COLUMN is used to:**

A. Add a new column
B. Change table name
C. Delete a column
D. Insert a row

---

---

# ✅ **Answer Key**

1-C
2-D
3-B
4-D
5-B
6-C
7-A
8-D
9-B
10-D (MySQL) / A also valid depending on SQL dialect
11-B
12-D
13-A
14-A
15-C
16-B
17-C
18-C
19-B
20-C
21-B
22-C
23-D
24-A
25-C
26-B
27-D
28-B
29-B
30-B
31-B
32-D
33-A
34-C
35-B
36-B
37-B
38-A
39-B
40-A

---

---

# 🔥 **40 Hard SQL MCQs (Advanced Level)**

---

### **1.** What will the query return?

`SELECT COUNT(*) FROM emp WHERE salary > ALL (SELECT salary FROM emp WHERE dept = 10);`
A. Employees whose salary is greater than every employee in dept 10
B. Employees whose salary is greater than some employee in dept 10
C. Employees whose salary equals max salary of dept 10
D. Employees in dept 10

---

### **2.** Which SQL clause is logically evaluated *last*?

A. SELECT
B. ORDER BY
C. HAVING
D. GROUP BY

---

### **3.** A correlated subquery is required when:

A. Outer query depends on subquery
B. Subquery depends on outer query
C. Both queries return same number of rows
D. Both use GROUP BY

---

### **4.** What does this return?

`SELECT department, SUM(salary) FROM emp GROUP BY department WITH ROLLUP;`
A. Only department totals
B. Only grand total
C. Department totals + grand total
D. Only detail rows

---

### **5.** Which condition is TRUE for NULL comparisons?

A. NULL = NULL
B. NULL <> NULL
C. NULL compares only with zero
D. NULL = NULL is UNKNOWN

---

### **6.** A window function requires:

A. GROUP BY
B. ORDER BY
C. OVER() clause
D. DISTINCT

---

### **7.** Which statement is correct about INTERSECT?

A. Returns duplicates
B. Removes duplicates
C. Returns duplicates twice
D. Same as INNER JOIN

---

### **8.** Which query finds second highest salary?

A. `SELECT MAX(salary) WHERE salary < MAX(salary)`
B. `SELECT MAX(salary) FROM emp WHERE salary NOT IN (SELECT MAX(salary) FROM emp);`
C. `SELECT SECOND(salary) FROM emp;`
D. `SELECT salary FROM emp LIMIT 2;`

---

### **9.** In FULL OUTER JOIN ON A.id = B.id, if both sides are NULL:

A. They match
B. They don’t match
C. They cause error
D. They are ignored

---

### **10.** Which SELECT is invalid?

A. SELECT DISTINCT COUNT(*)
B. SELECT COUNT(DISTINCT salary)
C. SELECT MAX(DISTINCT salary)
D. SELECT DISTINCT dept, COUNT(*) OVER()

---

### **11.** What does the following do?

`DELETE FROM emp WHERE id IN (SELECT id FROM emp);`
A. Deletes nothing
B. Deletes all rows
C. Deletes only NULL rows
D. Fails due to subquery reference

---

### **12.** Which is TRUE about TRUNCATE?

A. Always logged fully
B. Can be rolled back in all systems
C. Resets identity (auto-increment)
D. Works like DELETE with WHERE

---

### **13.** In SQL, a CROSS JOIN with a WHERE clause acts like:

A. FULL JOIN
B. INNER JOIN
C. OUTER JOIN
D. SELF JOIN

---

### **14.** Window function without PARTITION clause:

A. Fails
B. Applies to whole result set
C. Ignores ORDER BY
D. Computes only on NULL rows

---

### **15.** Which is TRUE about EXISTS vs IN?

A. EXISTS compares values
B. IN checks row existence
C. EXISTS stops early
D. IN stops early

---

### **16.** What does this return?

`SELECT SUM(CASE WHEN salary IS NULL THEN 1 ELSE 0 END) FROM emp;`
A. Total salaries
B. Total NULL salaries
C. Total non-NULL salaries
D. Invalid syntax

---

### **17.** Which statement returns employees with no department?

A. WHERE dept = NULL
B. WHERE dept IS NULL
C. WHERE dept = ''
D. WHERE dept <> NULL

---

### **18.** Which statement adds a NOT NULL constraint?

A. ALTER TABLE emp ADD CONSTRAINT NOT NULL salary;
B. ALTER TABLE emp MODIFY salary NOT NULL;
C. UPDATE emp SET salary NOT NULL;
D. CHANGE TABLE emp salary NOT NULL;

---

### **19.** Which returns the same result as `A EXCEPT B`?

A. A LEFT JOIN B WHERE B.id IS NULL
B. A INNER JOIN B
C. B LEFT JOIN A
D. A UNION B

---

### **20.** Which window function ranks ties with same rank number and skips ranks?

A. RANK()
B. DENSE_RANK()
C. ROW_NUMBER()
D. NTILE()

---

### **21.** A scalar subquery returns:

A. Any number of rows
B. Exactly one row, one column
C. Multiple columns
D. Only NULL

---

### **22.** GROUP BY on multiple columns groups rows by:

A. Either column
B. Both columns combined
C. Column with more distinct values
D. Only the first column

---

### **23.** Query:

`SELECT COUNT(*) FROM emp WHERE salary > (SELECT AVG(salary) FROM emp);`
A. Always returns half
B. Returns employees earning above average
C. Error: nested aggregates not allowed
D. Compares only max value

---

### **24.** What happens in:

`SELECT * FROM A JOIN B ON A.id = B.id WHERE B.id IS NULL;`
A. Returns full join
B. Returns no rows
C. Returns unmatched rows
D. Returns NULL id matches

---

### **25.** Which query lists employees making maximum salary in each department?

A. Use MAX() with GROUP BY
B. Use window function RANK() OVER(PARTITION BY dept ORDER BY salary DESC)
C. Use HAVING MAX(salary)
D. Use DISTINCT

---

### **26.** SET operator precedence is:

A. UNION > INTERSECT > EXCEPT
B. INTERSECT > UNION > EXCEPT
C. INTERSECT > EXCEPT > UNION
D. EXCEPT > INTERSECT > UNION

---

### **27.** Which is TRUE for ON vs WHERE in joins?

A. ON filters before join
B. WHERE filters after join
C. Both filter after join
D. ON can reference aliases from SELECT

---

### **28.** Which window function returns cumulative totals?

A. SUM() OVER(ORDER BY col)
B. SUM(col)
C. SUM() GROUP BY col
D. NTILE()

---

### **29.** CROSS APPLY is used to:

A. Join only matching rows
B. Apply table-valued function for each row
C. Filter NULL rows
D. Perform outer join

---

### **30.** What does DISTINCT ON (col) do? (PostgreSQL)

A. Removes duplicates across all columns
B. Removes duplicates only for col, keeping first row
C. Same as GROUP BY col
D. Same as UNIQUE

---

### **31.** Which is impossible inside a view?

A. SELECT with JOIN
B. SELECT with GROUP BY
C. ORDER BY without LIMIT
D. SELECT with window function

---

### **32.** What does this produce?

`SELECT dept, COUNT(*) OVER(PARTITION BY dept) FROM emp;`
A. Count per dept, repeated for every row
B. Count for whole table
C. Only distinct counts
D. NULL counts

---

### **33.** Which is TRUE about DELETE CASCADE?

A. Deletes parent rows only
B. Deletes children automatically
C. Deletes nothing
D. Triggers TRUNCATE

---

### **34.** Query:

`SELECT * FROM emp WHERE (dept, salary) IN (SELECT dept, MAX(salary) FROM emp GROUP BY dept);`
A. Invalid syntax
B. Employees earning max salary in their dept
C. Only max salary employees across company
D. Always empty

---

### **35.** When performing LEFT JOIN, rows from left table appear even if:

A. No match in right table
B. NULL join keys
C. ON condition fails
D. All of the above

---

### **36.** What does this compute?

`SELECT STDDEV(salary) FROM emp;`
A. Mean
B. Variance
C. Standard deviation
D. Median

---

### **37.** A composite index helps most when:

A. All columns are used in WHERE in same order
B. Only one column is used
C. Columns are not selective
D. Data is small

---

### **38.** Query:

`SELECT id FROM emp WHERE salary > (SELECT salary FROM emp WHERE id = 10);`
A. Correlated subquery
B. Non-correlated scalar subquery
C. Multiple-row subquery
D. Inline view

---

### **39.** A materialized view:

A. Stores no data
B. Stores query results physically
C. Cannot be refreshed
D. Is faster than table

---

### **40.** Which SQL clause can use both aggregate and non-aggregate columns?

A. SELECT
B. GROUP BY
C. HAVING
D. WHERE

---

---

# ✅ **Answer Key (Hard MCQs)**

1-A
2-B
3-B
4-C
5-D
6-C
7-B
8-B
9-B
10-A
11-B
12-C
13-B
14-B
15-C
16-B
17-B
18-B
19-A
20-A
21-B
22-B
23-B
24-B
25-B
26-C
27-A & B (correct conceptual pair → expected answer: A)
28-A
29-B
30-B
31-C
32-A
33-B
34-B
35-D
36-C
37-A
38-B
39-B
40-A

---

