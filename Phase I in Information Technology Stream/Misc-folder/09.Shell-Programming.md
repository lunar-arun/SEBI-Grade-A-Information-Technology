
---

# ✅ **30 Easy MSQ Questions – Shell Programming**

### **1. Which of the following are valid shell interpreters in Linux?**

A. `/bin/bash`
B. `/bin/sh`
C. `/bin/python`
D. `/bin/ksh`

### **2. Which commands are used to display the contents of a file?**

A. `cat`
B. `less`
C. `tail`
D. `pwd`

### **3. Which variables represent script arguments in shell?**

A. `$0`
B. `$1`
C. `$2`
D. `$@`

### **4. Which of the following are valid ways to declare variables in bash?**

A. `name=John`
B. `age=20`
C. `set age 20`
D. `city = Delhi`

### **5. Which of the following commands display the current working directory?**

A. `pwd`
B. `echo $PWD`
C. `whoami`
D. `cd`

### **6. Which statements are true about shell variables?**

A. No spaces allowed around `=`
B. Variables are strings by default
C. Use `$var` to access variables
D. Variables must be declared with `var` keyword

### **7. Which loops are supported in bash?**

A. `for`
B. `while`
C. `foreach`
D. `until`

### **8. Which commands are used to view the first few lines of a file?**

A. `head`
B. `tail`
C. `cat`
D. `sed -n 1,5p`

### **9. Which are valid test operators in an `if` condition?**

A. `-eq`
B. `-lt`
C. `=`
D. `!=`

### **10. Which commands are used to search text in files?**

A. `grep`
B. `fgrep`
C. `find` (with `-exec grep`)
D. `touch`

### **11. Which of the following are valid ways to make a script executable?**

A. `chmod +x script.sh`
B. `chmod 755 script.sh`
C. `bash script.sh`
D. `sh script.sh`

### **12. Which commands show running processes?**

A. `ps`
B. `top`
C. `ls`
D. `htop`

### **13. Which of these are valid exit statuses in shell?**

A. `0`
B. `1`
C. `2`
D. `255`

### **14. Which shells support command substitution?**

A. `bash`
B. `sh`
C. `zsh`
D. `csh`

### **15. Which commands are used for file permissions?**

A. `chmod`
B. `chown`
C. `chgrp`
D. `cp`

### **16. Which symbols represent logical operators in shell?**

A. `&&`
B. `||`
C. `!`
D. `&`

### **17. Which are valid ways to read user input?**

A. `read name`
B. `read -p "Enter:" val`
C. `input()`
D. `read -s password`

### **18. Which commands are used to list files?**

A. `ls`
B. `dir`
C. `ll`
D. `list`

### **19. Which keywords are part of an `if` statement in bash?**

A. `if`
B. `then`
C. `else`
D. `fi`

### **20. Which are valid loop termination commands?**

A. `break`
B. `continue`
C. `exit`
D. `return`

### **21. Which of the following are used for arithmetic operations?**

A. `$(( ))`
B. `expr`
C. `let`
D. `${}`

### **22. Which commands show disk usage?**

A. `df`
B. `du`
C. `free`
D. `top`

### **23. Which environment variables are common in Linux?**

A. `$PATH`
B. `$HOME`
C. `$USER`
D. `$SHELL`

### **24. Which characters are used for wildcard matching?**

A. `*`
B. `?`
C. `[]`
D. `{}`

### **25. Which commands can delete files?**

A. `rm`
B. `unlink`
C. `rmdir` (for empty directory)
D. `del`

### **26. Which options for `ls` are valid?**

A. `-l`
B. `-a`
C. `-h`
D. `-t`

### **27. Which redirection operators are valid in shell?**

A. `>`
B. `>>`
C. `<`
D. `|`

### **28. Which commands are used to display system date and time?**

A. `date`
B. `timedatectl`
C. `time`
D. `cal`

### **29. Which are valid methods to substitute variables in strings?**

A. `"Hello $name"`
B. `'Hello $name'`
C. `"Value is ${val}"`
D. `$name`

### **30. Which are valid file comparison operators?**

A. `-f`
B. `-d`
C. `-e`
D. `-s`

---

# ✅ **Answer Key (Table Format)**

| Q.No | Correct Options |
| ---- | --------------- |
| 1    | A, B, D         |
| 2    | A, B, C         |
| 3    | A, B, C, D      |
| 4    | A, B            |
| 5    | A, B            |
| 6    | A, B, C         |
| 7    | A, B, D         |
| 8    | A, D            |
| 9    | A, B, C, D      |
| 10   | A, B, C         |
| 11   | A, B            |
| 12   | A, B, D         |
| 13   | A, B, C, D      |
| 14   | A, B, C, D      |
| 15   | A, B, C         |
| 16   | A, B, C         |
| 17   | A, B, D         |
| 18   | A, B, C         |
| 19   | A, B, C, D      |
| 20   | A, B, C, D      |
| 21   | A, B, C         |
| 22   | A, B            |
| 23   | A, B, C, D      |
| 24   | A, B, C         |
| 25   | A, B            |
| 26   | A, B, C, D      |
| 27   | A, B, C, D      |
| 28   | A, B            |
| 29   | A, C, D         |
| 30   | A, B, C, D      |

---

---

# **40 Medium-Level MCQs – Shell Programming**

### **1. Which of the following commands will correctly assign the output of a command to a variable?**

A. `var=$(ls)`
B. `var=`ls``
C. `var=ls`
D. `var=$ls`

### **2. What is the output of `echo "$((5+3))"`?**

A. `5+3`
B. `8`
C. `$((5+3))`
D. `53`

### **3. Which of the following correctly checks if a file exists in bash?**

A. `[ -f filename ]`
B. `[ -e filename ]`
C. `[[ -d filename ]]`
D. `[ file filename ]`

### **4. Which loop will execute at least once regardless of the condition?**

A. `while`
B. `for`
C. `until`
D. `do-while`

*(Note: `do-while` is not natively in bash, trick question for careful reading.)*

### **5. Which of the following is true about `$?` in shell scripting?**

A. Stores the PID of last process
B. Stores exit status of last command
C. Stores the number of arguments
D. Stores last variable value

### **6. What does the command `cut -d ":" -f1 /etc/passwd` do?**

A. Cuts first character from each line
B. Prints first field (username) separated by `:`
C. Deletes the first line
D. Replaces `:` with whitespace

### **7. Which syntax correctly checks multiple conditions in bash?**

A. `[ "$a" -eq 1 -a "$b" -eq 2 ]`
B. `[[ "$a" -eq 1 && "$b" -eq 2 ]]`
C. `[ "$a" -eq 1 && "$b" -eq 2 ]`
D. `[ "$a" -eq 1 -o "$b" -eq 2 ]`

### **8. Which of these will append output to a file without overwriting?**

A. `echo "text" > file.txt`
B. `echo "text" >> file.txt`
C. `cat file.txt >> file.txt`
D. `echo "text" >| file.txt`

### **9. What is the purpose of `shift` in shell scripting?**

A. Shifts contents of an array
B. Moves positional parameters to left
C. Exits the script
D. Changes variable type

### **10. How do you declare an array in bash?**

A. `arr=(1 2 3)`
B. `arr[0]=1 arr[1]=2`
C. `declare -a arr`
D. All of the above

### **11. Which command displays only the last 10 lines of a file by default?**

A. `tail filename`
B. `head filename`
C. `cat filename | tail`
D. Both A & C

### **12. How do you check if a string is empty in bash?**

A. `[ -z "$str" ]`
B. `[ -n "$str" ]`
C. `[ "$str" = "" ]`
D. Both A & C

### **13. Which command will replace text in a file using `sed`?**

A. `sed 's/old/new/' file`
B. `sed 's/old/new/g' file`
C. `sed -i 's/old/new/g' file`
D. All of the above

### **14. Which of the following will correctly loop through all `.txt` files in a directory?**

A. `for file in *.txt; do echo $file; done`
B. `for $file in *.txt; do echo $file; done`
C. `foreach file in *.txt; do echo $file; done`
D. `for file *.txt; do echo $file; done`

### **15. What does `expr 5 + 4` return?**

A. `9`
B. `5+4`
C. `$((5+4))`
D. Error

### **16. Which command checks for directory existence?**

A. `[ -f dir ]`
B. `[ -d dir ]`
C. `[ -e dir ]`
D. `[ dir ]`

### **17. What is the difference between `>` and `>>` in shell?**

A. `>` appends, `>>` overwrites
B. `>` overwrites, `>>` appends
C. Both overwrite
D. Both append

### **18. Which of the following prints all shell arguments?**

A. `$*`
B. `$@`
C. `$#`
D. `$0`

### **19. Which command counts the number of lines in a file?**

A. `wc -l filename`
B. `wc -w filename`
C. `cat filename | wc -l`
D. Both A & C

### **20. Which command will list files sorted by modification time?**

A. `ls -lt`
B. `ls -ltr`
C. `ls -lh`
D. `ls -a`

### **21. How do you test if two strings are equal in bash?**

A. `[ "$a" == "$b" ]`
B. `[ $a = $b ]`
C. `[[ "$a" == "$b" ]]`
D. All of the above

### **22. Which of the following prints the PID of the current shell?**

A. `$$`
B. `$!`
C. `$?`
D. `$#`

### **23. Which command finds files by name recursively?**

A. `find /path -name "*.txt"`
B. `grep -r "filename"`
C. `locate filename`
D. `ls -R | grep filename`

### **24. Which command displays the first 20 lines of a file?**

A. `head -20 filename`
B. `tail -20 filename`
C. `cat filename | head -20`
D. Both A & C

### **25. What does `chmod 755 file.sh` do?**

A. Owner can read/write/execute
B. Group can read & execute
C. Others can read & execute
D. All of the above

### **26. How to get the length of a string in bash?**

A. `${#str}`
B. `expr length "$str"`
C. `echo $str | wc -c`
D. All of the above

### **27. Which of these commands will terminate a process by PID?**

A. `kill PID`
B. `kill -9 PID`
C. `pkill processname`
D. All of the above

### **28. Which command prints the last 5 lines of a file?**

A. `tail -5 filename`
B. `head -5 filename`
C. `cat filename | tail -5`
D. Both A & C

### **29. Which of the following is true about `$#`?**

A. Stores total number of script arguments
B. Stores first argument
C. Stores exit status
D. Stores last argument

### **30. How do you define a function in bash?**

A. `function myfunc { commands; }`
B. `myfunc() { commands; }`
C. `def myfunc() { commands; }`
D. Both A & B

### **31. Which command displays environment variables?**

A. `env`
B. `printenv`
C. `set`
D. All of the above

### **32. Which command replaces all spaces in a file with underscores using `sed`?**

A. `sed 's/ /_/g' file`
B. `sed 's/\s/_/g' file`
C. `tr ' ' '_' < file`
D. Both A & C

### **33. Which command counts words in a file?**

A. `wc -w file`
B. `wc -l file`
C. `cat file | wc -w`
D. Both A & C

### **34. How to make a script exit with a custom status?**

A. `exit 0`
B. `return 1`
C. `exit 5`
D. Both A & C

### **35. Which command extracts columns from a CSV file?**

A. `cut -d ',' -f2 file.csv`
B. `awk -F ',' '{print $2}' file.csv`
C. `grep ',' file.csv`
D. Both A & B

### **36. What does `read -p "Enter name: " name` do?**

A. Prompts user with "Enter name:" and stores input in `name`
B. Reads input silently
C. Reads arguments passed to script
D. Prints `name`

### **37. Which command substitutes multiple spaces with a single space?**

A. `tr -s ' '`
B. `sed 's/  / /g'`
C. `awk '{$1=$1; print}'`
D. All of the above

### **38. Which command counts occurrences of a pattern in a file?**

A. `grep -c "pattern" file`
B. `grep "pattern" file | wc -l`
C. `awk '/pattern/ {count++} END {print count}' file`
D. All of the above

### **39. Which of the following will correctly export a variable?**

A. `export VAR=value`
B. `VAR=value; export VAR`
C. `set VAR=value`
D. Both A & B

### **40. How do you remove all files ending with `.log` in a directory safely?**

A. `rm *.log`
B. `rm -i *.log`
C. `find . -name "*.log" -exec rm {} \;`
D. Both B & C

---

# ✅ **Answer Key (Table Format)**

| Q.No | Correct Options                              |
| ---- | -------------------------------------------- |
| 1    | A, B                                         |
| 2    | B                                            |
| 3    | A, B                                         |
| 4    | Trick question – None (bash has no do-while) |
| 5    | B                                            |
| 6    | B                                            |
| 7    | A, B, D                                      |
| 8    | B                                            |
| 9    | B                                            |
| 10   | D                                            |
| 11   | D                                            |
| 12   | D                                            |
| 13   | D                                            |
| 14   | A                                            |
| 15   | A                                            |
| 16   | B                                            |
| 17   | B                                            |
| 18   | A, B                                         |
| 19   | D                                            |
| 20   | A                                            |
| 21   | D                                            |
| 22   | A                                            |
| 23   | A                                            |
| 24   | D                                            |
| 25   | D                                            |
| 26   | D                                            |
| 27   | D                                            |
| 28   | D                                            |
| 29   | A                                            |
| 30   | D                                            |
| 31   | D                                            |
| 32   | D                                            |
| 33   | D                                            |
| 34   | D                                            |
| 35   | D                                            |
| 36   | A                                            |
| 37   | D                                            |
| 38   | D                                            |
| 39   | D                                            |
| 40   | D                                            |

---

---

# **50 Hard-Level MCQs – Shell Programming**

### **1. What will be the output of the following script?**

```bash
a=5
b=3
echo $((a**b))
```

A. `8`
B. `15`
C. `125`
D. `Error`

### **2. How do you check if a variable is a number in bash?**

A. `[ $var -eq $var ]`
B. `[[ $var =~ ^[0-9]+$ ]]`
C. `expr $var + 0`
D. All of the above

### **3. What does `IFS` stand for in bash scripting?**

A. Internal Field Separator
B. Internal File System
C. Input File Separator
D. Index Field Symbol

### **4. What is the output of:**

```bash
arr=(1 2 3 4)
echo ${arr[@]:1:2}
```

A. `1 2`
B. `2 3`
C. `3 4`
D. `2 3 4`

### **5. Which of the following is true about `trap` in bash?**

A. It catches signals
B. Syntax: `trap "commands" SIGINT`
C. Can execute cleanup code before script exits
D. All of the above

### **6. What does `$!` represent in bash?**

A. PID of last background process
B. Exit status of last command
C. Number of arguments
D. Current shell PID

### **7. How can you prevent word splitting when expanding a variable?**

A. Use double quotes: `"$var"`
B. Use single quotes: `'$var'`
C. Use `eval $var`
D. Use backticks: `` `var` ``

### **8. What is the difference between `[ ]` and `[[ ]]` in bash?**

A. `[ ]` is POSIX test; `[[ ]]` is bash extension
B. `[ ]` allows regex matching; `[[ ]]` does not
C. `[ ]` supports `&&`/`||` inside; `[[ ]]` does not
D. None of the above

### **9. Which command shows hidden files in a directory?**

A. `ls -a`
B. `ls -A`
C. `ls -l`
D. Both A & B

### **10. What does this script print?**

```bash
for i in {1..5}; do
  ((i%2==0)) && echo $i
done
```

A. `1 3 5`
B. `2 4`
C. `1 2 3 4 5`
D. Nothing

### **11. Which command finds files modified in the last 7 days?**

A. `find /path -mtime -7`
B. `find /path -mtime 7`
C. `ls -lt | head -7`
D. `grep -mtime 7 /path`

### **12. How to redirect both stdout and stderr to a file?**

A. `command > file 2>&1`
B. `command &> file`
C. `command > file`
D. Both A & B

### **13. Which of these expressions correctly evaluates floating-point numbers in bash?**

A. `echo "5/2" | bc`
B. `awk 'BEGIN{print 5/2}'`
C. `expr 5 / 2`
D. Both A & B

### **14. How do you check if a variable is unset or empty?**

A. `[ -z "$var" ]`
B. `[ -n "$var" ]`
C. `[ ! $var ]`
D. `[ -v var ]`

### **15. Which of the following removes all non-empty directories recursively?**

A. `rm -r dir`
B. `rmdir dir`
C. `rm -rf dir`
D. Both A & C

### **16. Which of the following checks if a file is readable?**

A. `[ -r filename ]`
B. `[ -w filename ]`
C. `[ -x filename ]`
D. `[ -f filename ]`

### **17. What is the difference between `source file` and `. file`?**

A. `source file` executes in current shell
B. `. file` executes in current shell
C. Both are equivalent in bash
D. `source file` runs in subshell

### **18. How do you pass arrays as arguments to functions in bash?**

A. By reference using global arrays
B. By passing elements as separate arguments
C. Using `"$@"` and `"$*"`
D. Bash does not support passing arrays

### **19. Which command finds the number of occurrences of "error" in a log file?**

A. `grep -c "error" logfile`
B. `grep "error" logfile | wc -l`
C. `awk '/error/{count++} END{print count}' logfile`
D. All of the above

### **20. Which of the following is true about `read -a array`?**

A. Reads input into an array
B. Uses default IFS
C. Can be used in loops
D. All of the above

### **21. What is the output of:**

```bash
echo ${VAR:-default}
```

A. Prints VAR value if set, else prints `default`
B. Always prints `default`
C. Throws an error if VAR is unset
D. Prints empty string

### **22. Which command finds files larger than 1MB?**

A. `find /path -size +1M`
B. `find /path -size 1M`
C. `ls -lh | awk '{print $5 > 1MB}'`
D. `du -h | grep 1M`

### **23. Which of the following correctly replaces `foo` with `bar` in all `.txt` files?**

A. `sed -i 's/foo/bar/g' *.txt`
B. `perl -pi -e 's/foo/bar/g' *.txt`
C. `awk '{gsub(/foo/,"bar"); print}' *.txt`
D. All of the above

### **24. How do you find the last 5 lines of a file in reverse order?**

A. `tail -5 file | tac`
B. `tail -5 file | sort -r`
C. `head -5 file | tac`
D. `tail -n 5 -r file`

### **25. What does `${VAR#pattern}` do?**

A. Removes shortest match of pattern from start
B. Removes longest match of pattern from start
C. Removes shortest match of pattern from end
D. Removes longest match of pattern from end

### **26. How do you run a command only if the previous command fails?**

A. `cmd1 || cmd2`
B. `cmd1 && cmd2`
C. `if cmd1; then cmd2; fi`
D. `cmd1 ; cmd2`

### **27. Which of the following waits for a background process to complete?**

A. `wait PID`
B. `fg`
C. `jobs -w`
D. `kill -0 PID`

### **28. How do you execute a script in a new shell?**

A. `bash script.sh`
B. `sh script.sh`
C. `./script.sh` (if executable)
D. All of the above

### **29. Which of these is true about positional parameters after `shift 2`?**

A. `$1` now holds the 3rd argument
B. `$#` decreases by 2
C. `$0` changes
D. Both A & B

### **30. Which command extracts a column from CSV using `awk`?**

A. `awk -F',' '{print $2}' file.csv`
B. `cut -d ',' -f2 file.csv`
C. `sed 's/,/ /g' file.csv`
D. Both A & B

### **31. Which option for `grep` ignores case?**

A. `-i`
B. `-v`
C. `-c`
D. `-r`

### **32. Which command prints only unique lines from a file?**

A. `uniq file`
B. `sort file | uniq`
C. `awk '!seen[$0]++' file`
D. All of the above

### **33. What is the output of:**

```bash
x=5
(( x++ > 5 )) && echo yes || echo no
```

A. `yes`
B. `no`
C. `5`
D. Nothing

### **34. Which of the following reads a file line by line safely?**

A. `while IFS= read -r line; do ... done < file`
B. `for line in $(cat file); do ... done`
C. `while read line; do ... done < file`
D. Both A & C

### **35. How do you remove all trailing whitespace from a file?**

A. `sed -i 's/[ \t]*$//' file`
B. `awk '{$1=$1; print}' file`
C. `tr -d ' \t' file`
D. Both A & B

### **36. What is the difference between `$*` and `$@` in quotes?**

A. "$*" expands as a single string
B. "$@" expands as separate arguments
C. Both behave the same without quotes
D. All of the above

### **37. Which command substitutes tabs with spaces?**

A. `expand file`
B. `tr '\t' ' '`
C. `sed 's/\t/ /g' file`
D. All of the above

### **38. Which of these signals cannot be trapped in bash?**

A. SIGINT
B. SIGKILL
C. SIGTERM
D. SIGHUP

### **39. How do you reverse a string in bash?**

A. `echo "str" | rev`
B. `awk '{ for(i=length;i!=0;i--) printf "%s", substr($0,i,1) }' <<< "str"`
C. `rev <<< "str"`
D. All of the above

### **40. Which command outputs the top 5 memory-consuming processes?**

A. `ps aux --sort=-%mem | head -5`
B. `top -b -o +%MEM | head -5`
C. `htop | sort -m`
D. Both A & B

### **41. How do you check if a directory is empty?**

A. `[ -z "$(ls -A dir)" ]`
B. `[ "$(ls -A dir)" = "" ]`
C. `[ -s dir ]`
D. Both A & B

### **42. Which of the following will split a string into an array by comma?**

A. `IFS=',' read -ra arr <<< "$str"`
B. `arr=(${str//,/ })`
C. `arr=$(echo $str | tr ',' ' ')`
D. Both A & B

### **43. Which command counts unique words in a file?**

A. `tr -s ' ' '\n' < file | sort | uniq -c`
B. `awk '{for(i=1;i<=NF;i++) count[$i]++} END{for(w in count) print w,count[w]}' file`
C. `grep -o -E '\w+' file | sort | uniq -c`
D. All of the above

### **44. How do you test multiple OR conditions in `[[ ]]`?**

A. `[[ "$a" -eq 1 || "$b" -eq 2 ]]`
B. `[ "$a" -eq 1 -o "$b" -eq 2 ]`
C. `[[ "$a" -eq 1 -o "$b" -eq 2 ]]`
D. Both A & B

### **45. Which command shows which functions are defined in bash?**

A. `declare -F`
B. `typeset -f`
C. `functions`
D. All of the above

### **46. How do you create a here-document in bash?**

A. `cat << EOF`
B. `cat <<< "text"`
C. `echo << EOF`
D. Both A & B

### **47. Which command prints last modification time of a file?**

A. `stat -c %y file`
B. `ls -l file`
C. `date file`
D. Both A & B

### **48. Which command replaces all newline characters with spaces?**

A. `tr '\n' ' ' < file`
B. `awk '{printf "%s ", $0}' file`
C. `paste -s -d' ' file`
D. All of the above

### **49. Which command finds symbolic links in a directory?**

A. `find /path -type l`
B. `ls -l | grep '^l'`
C. `stat -c %N *`
D. Both A & B

### **50. How do you make a script exit on first error?**

A. `set -e`
B. `set -o errexit`
C. `trap 'exit' ERR`
D. Both A & B

---

# ✅ **Answer Key (Hard-Level)**

| Q.No | Correct Options |
| ---- | --------------- |
| 1    | C               |
| 2    | D               |
| 3    | A               |
| 4    | B               |
| 5    | D               |
| 6    | A               |
| 7    | A               |
| 8    | A               |
| 9    | D               |
| 10   | B               |
| 11   | A               |
| 12   | D               |
| 13   | D               |
| 14   | A               |
| 15   | D               |
| 16   | A               |
| 17   | C               |
| 18   | D               |
| 19   | D               |
| 20   | D               |
| 21   | A               |
| 22   | A               |
| 23   | D               |
| 24   | A               |
| 25   | A               |
| 26   | A               |
| 27   | A               |
| 28   | D               |
| 29   | D               |
| 30   | D               |
| 31   | A               |
| 32   | D               |
| 33   | B               |
| 34   | A               |
| 35   | D               |
| 36   | D               |
| 37   | D               |
| 38   | B               |
| 39   | D               |
| 40   | D               |
| 41   | D               |
| 42   | D               |
| 43   | D               |
| 44   | D               |
| 45   | D               |
| 46   | D               |
| 47   | D               |
| 48   | D               |
| 49   | D               |
| 50   | D               |

---

## 🔥 Hard-Level MCQs: Shell Programming (SEBI Grade A)

### **Q1.**

What will be the output of the following script?

```sh
x=5
y=$x+2
echo $y
```

A. 7
B. 5+2
C. Error
D. Blank output

---

### **Q2.**

Consider the script:

```sh
#!/bin/sh
a=10
b=20
if [ $a > $b ]
then
  echo "A"
else
  echo "B"
fi
```

What will be printed?

A. A
B. B
C. Error
D. Depends on shell

---

### **Q3.**

Which of the following is **TRUE** regarding positional parameters in shell?

A. `$0` stores the first argument
B. `$#` gives the number of arguments
C. `$*` and `$@` behave identically in all cases
D. `$?` stores the process ID

---

### **Q4.**

What is the output?

```sh
for i in 1 2 3
do
  echo $i
  break
done
```

A. 1 2 3
B. 1
C. No output
D. Infinite loop

---

### **Q5.**

Which command will correctly check **if a file exists and is readable**?

A. `[ -f file ]`
B. `[ -r file ]`
C. `[ -f file -r ]`
D. `[ -f file ] && [ -r file ]`

---

### **Q6.**

What does the following return?

```sh
false
echo $?
```

A. 0
B. 1
C. -1
D. Depends on OS

---

### **Q7.**

Which statement about `return` in shell scripts is **correct**?

A. `return` can be used only in scripts
B. `return` can return strings
C. `return` exits a function with a numeric status
D. `return` behaves like `exit` always

---

### **Q8.**

What is the output?

```sh
x="hello world"
set $x
echo $1
```

A. hello world
B. hello
C. world
D. Error

---

### **Q9.**

Which command replaces **current shell process** with another command?

A. fork
B. exec
C. subshell
D. source

---

### **Q10.**

What will this script print?

```sh
i=0
while [ $i -lt 3 ]
do
  echo $i
  i=`expr $i + 1`
done
```

A. 0 1 2
B. 1 2 3
C. 0 1 2 3
D. Infinite loop

---

### **Q11.**

Which UNIX command is used to **count number of lines, words, and characters**?

A. count
B. grep
C. wc
D. awk

---

### **Q12.**

What does `$@` expand to when quoted (`"$@"`)?

A. All arguments as one string
B. Each argument as a separate string
C. Only first argument
D. No arguments

---

### **Q13.**

Which of the following is **NOT** a valid way to assign a variable?

A. `x=10`
B. `x = 10`
C. `x=$(expr 5 + 5)`
D. `x=\`expr 5 + 5``

---

### **Q14.**

What is the output?

```sh
echo "Today is `date | cut -d' ' -f1`"
```

A. Today is date
B. Today is current day name
C. Today is full date
D. Syntax error

---

### **Q15.**

Which command is used to **change file permissions**?

A. chown
B. chmod
C. umask
D. passwd

---

---

## ✅ **Answers**

1. **B**
2. **A** (string comparison, `>` redirects output)
3. **B**
4. **B**
5. **D**
6. **B**
7. **C**
8. **B**
9. **B**
10. **A**
11. **C**
12. **B**
13. **B**
14. **B**
15. **B**

---


