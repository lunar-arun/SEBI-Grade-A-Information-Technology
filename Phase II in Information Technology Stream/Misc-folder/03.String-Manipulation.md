
---

### **Python String Manipulation MCQs**

**1. What is the length of the string `"SEBI2025"`?**
A) 6
B) 7
C) 8
D) 9

**2. Which of the following will get the first 3 characters of `s = "SEBIGrade"`?**
A) `s[0:3]`
B) `s[1:4]`
C) `s[:4]`
D) `s[3:6]`

**3. What does `len("")` return?**
A) 0
B) 1
C) None
D) Error

**4. Which function removes whitespace from the beginning and end of a string `s`?**
A) `s.strip()`
B) `s.remove()`
C) `s.trim()`
D) `s.delete()`

**5. What does `"python".upper()` return?**
A) `"python"`
B) `"Python"`
C) `"PYTHON"`
D) `"pyTHon"`

**6. How to check if a string `s` contains only digits?**
A) `s.isdigit()`
B) `s.isalpha()`
C) `s.isalnum()`
D) `s.checkdigit()`

**7. Which expression returns `"Grade"` from `s = "SEBIGradeA"`?**
A) `s[4:9]`
B) `s[3:8]`
C) `s[5:10]`
D) `s[4:8]`

**8. What does `"hello world".find("world")` return?**
A) 0
B) 6
C) 5
D) -1

**9. Which method replaces `"SEBI"` with `"RBI"` in `s = "SEBI2025"`?**
A) `s.replace("SEBI","RBI")`
B) `s.sub("SEBI","RBI")`
C) `s.update("SEBI","RBI")`
D) `s.change("SEBI","RBI")`

**10. What is the output of `"SEBI2025".startswith("SE")`?**
A) True
B) False
C) Error
D) None

**11. What does `"SEBI2025".endswith("25")` return?**
A) True
B) False
C) Error
D) None

**12. Which regex pattern finds all digits in `"SEBI2025"`?**
A) `\d`
B) `\w`
C) `\s`
D) `.`

**13. Which method splits `"SEBI,Grade,A"` by comma?**
A) `"SEBI,Grade,A".split(",")`
B) `"SEBI,Grade,A".divide(",")`
C) `"SEBI,Grade,A".cut(",")`
D) `"SEBI,Grade,A".break(",")`

**14. How to join a list `["SEBI","Grade","A"]` into a single string separated by space?**
A) `" ".join(["SEBI","Grade","A"])`
B) `" ".merge(["SEBI","Grade","A"])`
C) `" ".combine(["SEBI","Grade","A"])`
D) `" ".append(["SEBI","Grade","A"])`

**15. What does `"Python".lower()` return?**
A) `"Python"`
B) `"PYTHON"`
C) `"python"`
D) `"PyThOn"`

**16. What is the output of `"abc123".isalnum()`?**
A) True
B) False
C) Error
D) None

**17. What will `"abc123".isalpha()` return?**
A) True
B) False
C) Error
D) None

**18. Which function counts occurrences of `"a"` in `"banana"`?**
A) `"banana".count("a")`
B) `"banana".find("a")`
C) `"banana".index("a")`
D) `"banana".search("a")`

**19. What is the output of `"   SEBI   ".lstrip()`?**
A) `"SEBI   "`
B) `"   SEBI"`
C) `"SEBI"`
D) Error

**20. What is the output of `"   SEBI   ".rstrip()`?**
A) `"SEBI   "`
B) `"   SEBI"`
C) `"SEBI"`
D) Error

**21. Which regex finds all letters in `"SEBI2025"`?**
A) `[A-Za-z]`
B) `\d`
C) `\s`
D) `[0-9]`

**22. Which method returns the index of `"B"` in `"SEBI"`?**
A) `"SEBI".index("B")`
B) `"SEBI".find("B")`
C) Both A and B
D) None

**23. What does `"SEBI2025"[2]` return?**
A) `"S"`
B) `"B"`
C) `"E"`
D) `"I"`

**24. What will `"hello".replace("l","L")` return?**
A) `"heLLo"`
B) `"heLlo"`
C) `"heLLo"`
D) `"hello"`

**25. What does `"SEBI2025".isalnum()` return?**
A) True
B) False
C) Error
D) None

**26. Which method capitalizes only the first letter of a string?**
A) `"sebi".capitalize()`
B) `"sebi".title()`
C) `"sebi".upper()`
D) `"sebi".lower()`

**27. What will `"SEBI2025".replace("2025","2026")` return?**
A) `"SEBI2025"`
B) `"SEBI2026"`
C) `"SEBI"`
D) `"2026SEBI"`

**28. Which method checks if string contains only whitespace?**
A) `s.isspace()`
B) `s.isdigit()`
C) `s.isalpha()`
D) `s.isalnum()`

**29. Which slice returns the last 3 characters of `"SEBI2025"`?**
A) `s[-3:]`
B) `s[-2:]`
C) `s[-4:]`
D) `s[-5:]`

**30. What is the output of `"abc\nxyz".splitlines()`?**
A) `['abc', 'xyz']`
B) `['abc\nxyz']`
C) `['abc', '\n', 'xyz']`
D) `['abc xyz']`

**31. What does `"SEBI".rfind("B")` return?**
A) 0
B) 1
C) 2
D) -1

**32. Which regex pattern matches exactly 4 digits?**
A) `\d{4}`
B) `\d{3}`
C) `\d+`
D) `\w{4}`

**33. How to check if string `s` contains `"SEBI"` anywhere?**
A) `"SEBI" in s`
B) `s.has("SEBI")`
C) `s.contains("SEBI")`
D) `s.search("SEBI")`

**34. What is the output of `"Python".title()`?**
A) `"Python"`
B) `"PYTHON"`
C) `"python"`
D) `"PythOn"`

**35. Which method returns True if all characters are lowercase?**
A) `s.islower()`
B) `s.isupper()`
C) `s.isalpha()`
D) `s.isdigit()`

---

### **Answer Key Table**

| Q# | Answer |
| -- | ------ |
| 1  | C      |
| 2  | A      |
| 3  | A      |
| 4  | A      |
| 5  | C      |
| 6  | A      |
| 7  | A      |
| 8  | B      |
| 9  | A      |
| 10 | A      |
| 11 | A      |
| 12 | A      |
| 13 | A      |
| 14 | A      |
| 15 | C      |
| 16 | A      |
| 17 | B      |
| 18 | A      |
| 19 | A      |
| 20 | B      |
| 21 | A      |
| 22 | C      |
| 23 | B      |
| 24 | B      |
| 25 | A      |
| 26 | A      |
| 27 | B      |
| 28 | A      |
| 29 | A      |
| 30 | A      |
| 31 | C      |
| 32 | A      |
| 33 | A      |
| 34 | A      |
| 35 | A      |

---

---

## **Python String Manipulation – Medium MCQs (40 Questions)**

---

**1. What is the output of:**

```python
s = "SEBI2025"
print(s[1:-1])
```

A) `"EBI202"`
B) `"EBI2025"`
C) `"SEBI202"`
D) `"EBI25"`

**2. What does this code return?**

```python
s = "SEBI2025"
print(s[-4:-1])
```

A) `"202"`
B) `"2025"`
C) `"025"`
D) `"BI2"`

**3. What is the output of:**

```python
s = "SEBI2025"
print(s[::2])
```

A) `"SBI05"`
B) `"SEB2"`
C) `"SBI25"`
D) `"SB22"`

**4. Which statement checks if `"2025"` is at the end of the string `s`?**
A) `s.endswith("2025")`
B) `"2025" in s`
C) `s.isdigit()`
D) `s.find("2025")`

**5. What will the following code output?**

```python
import re
re.findall(r"[A-Z]+", "SeBi2025")
```

A) `['S', 'B']`
B) `['SEBI']`
C) `['SeBi']`
D) `['S', 'B', 'I']`

**6. Which expression extracts `"Grade"` from:**

```python
s = "SEBIGradeA"
```

A) `s[4:-1]`
B) `s[4:9]`
C) `s[5:10]`
D) `s[3:8]`

**7. Output of:**

```python
"SEBI2025".replace("2","X",1)
```

A) `"SEXBI2025"`
B) `"SEXBI2025"`
C) `"SEXBIX025"`
D) `"SEXBI2025"`

**8. Which method converts `"SEBI2025"` to a list of characters?**
A) `list("SEBI2025")`
B) `"SEBI2025".split()`
C) `"SEBI2025".chars()`
D) `"SEBI2025".toList()`

**9. Which code counts how many times `"e"` appears in `"SEBI2025"` ignoring case?**
A) `"SEBI2025".lower().count("e")`
B) `"SEBI2025".count("e")`
C) `re.count("e","SEBI2025")`
D) `"SEBI2025".upper().count("E")`

**10. Output of:**

```python
"banana".rfind("a")
```

A) 5
B) 4
C) 2
D) 1

**11. Which code returns `"SEBI-2025"` from:**

```python
s = "SEBI 2025"
```

A) `s.replace(" ","-")`
B) `s.sub(" ","-")`
C) `s.replace("","-")`
D) `s.split("-")`

**12. Output of:**

```python
s = "SEBI2025"
print(s[::3])
```

A) `"S2"`
B) `"S22"`
C) `"S25"`
D) `"S2I"`

**13. Which regex pattern matches both letters and digits in `"SEBI2025"`?**
A) `\w`
B) `\d`
C) `[A-Z]`
D) `[0-9]`

**14. What is returned by:**

```python
"abc123".isalnum()
```

A) True
B) False
C) Error
D) None

**15. Which expression splits `"SEBI,Grade,A"` by comma and trims spaces?**
A) `[x.strip() for x in "SEBI,Grade,A".split(",")]`
B) `"SEBI,Grade,A".split(",")`
C) `"SEBI,Grade,A".strip(",")`
D) `"SEBI,Grade,A".split(" ")`

**16. Output of:**

```python
s = "SEBI2025"
print(s[::-1])
```

A) `"5202IBES"`
B) `"SEBI2025"`
C) `"5202SEBI"`
D) `"SIB2025E"`

**17. What does `"SEBI2025".find("X")` return?**
A) -1
B) 0
C) None
D) Error

**18. Which method capitalizes the first letter of each word?**
A) `"sebi grade".title()`
B) `"sebi grade".capitalize()`
C) `"sebi grade".upper()`
D) `"sebi grade".lower()`

**19. Output of:**

```python
"   SEBI   ".strip()
```

A) `"SEBI"`
B) `"   SEBI"`
C) `"SEBI   "`
D) `"   SEBI   "`

**20. Which regex matches exactly 2 digits?**
A) `\d{2}`
B) `\d{1,2}`
C) `\d{3}`
D) `\d+`

**21. Output of:**

```python
"Python3.9".split(".")
```

A) `['Python3', '9']`
B) `['Python', '3', '9']`
C) `['Python3.9']`
D) `['Python3','9.']`

**22. What does `"SEBI2025".isdigit()` return?**
A) False
B) True
C) None
D) Error

**23. Which code checks if string starts with either `"S"` or `"R"`?**
A) `s.startswith(("S","R"))`
B) `s.startswith("S","R")`
C) `"S" in s or "R" in s`
D) `s.find("S","R")`

**24. Output of:**

```python
"SEBI2025".partition("202")
```

A) `('SEBI', '202', '5')`
B) `('SEBI2025', '', '')`
C) `('SEBI', '', '2025')`
D) `('SEBI', '2', '025')`

**25. Which method removes only trailing whitespace?**
A) `.rstrip()`
B) `.lstrip()`
C) `.strip()`
D) `.remove()`

**26. Which code checks if `"abc123"` contains letters?**
A) `any(c.isalpha() for c in "abc123")`
B) `"abc123".isalpha()`
C) `"abc123".isdigit()`
D) `all(c.isdigit() for c in "abc123")`

**27. Output of:**

```python
s = "Hello World"
print(s.count("l", 3, 8))
```

A) 2
B) 3
C) 1
D) 0

**28. Which code replaces all digits with `"#"` in `"SEBI2025"`?**
A) `re.sub(r"\d","#","SEBI2025")`
B) `"SEBI2025".replace("\d","#")`
C) `"SEBI2025".sub("\d","#")`
D) `re.replace(r"\d","#","SEBI2025")`

**29. What does `"SEBI".center(10,"*")` return?**
A) `"***SEBI***"`
B) `"****SEBI**"`
C) `"**SEBI***"`
D) `"SEBI******"`

**30. Output of:**

```python
"Python".swapcase()
```

A) `"pYTHON"`
B) `"Python"`
C) `"PYTHON"`
D) `"pyThon"`

**31. Which regex matches a lowercase letter followed by a digit?**
A) `[a-z]\d`
B) `\d[a-z]`
C) `[A-Z]\d`
D) `\w\d`

**32. Output of:**

```python
"abc123xyz".isnumeric()
```

A) False
B) True
C) Error
D) None

**33. Output of:**

```python
"abc123".zfill(8)
```

A) `"00abc123"`
B) `"000abc123"`
C) `"abc12300"`
D) `"abc123"`

**34. Which code removes newline characters from `"Hello\nWorld"`?**
A) `"Hello\nWorld".replace("\n","")`
B) `"Hello\nWorld".strip()`
C) `"Hello\nWorld".splitlines()`
D) `"Hello\nWorld".split("\n")`

**35. Output of:**

```python
"Python".endswith(("on","in"))
```

A) True
B) False
C) Error
D) None

**36. Which expression extracts every second character of `"SEBI2025"` starting from index 1?**
A) `s[1::2]`
B) `s[::2]`
C) `s[2::2]`
D) `s[1:2]`

**37. Output of:**

```python
re.findall(r"[0-9]{2}", "SEBI2025")
```

A) `['20','25']`
B) `['2','0','2','5']`
C) `['2025']`
D) `['20']`

**38. Which code checks if a string contains only letters?**
A) `"SEBI".isalpha()`
B) `"SEBI2025".isalpha()`
C) `"123".isalpha()`
D) `"SEBI 2025".isalpha()`

**39. Output of:**

```python
"Python".find("on")
```

A) 4
B) 3
C) 2
D) -1

**40. Which code counts occurrences of `"l"` ignoring case in `"Hello World"`?**
A) `"Hello World".lower().count("l")`
B) `"Hello World".count("l")`
C) `"Hello World".upper().count("L")`
D) Both A and C

---

### **Answer Key Table (40 MCQs)**

| Q# | Answer |
| -- | ------ |
| 1  | A      |
| 2  | A      |
| 3  | C      |
| 4  | A      |
| 5  | A      |
| 6  | B      |
| 7  | D      |
| 8  | A      |
| 9  | D      |
| 10 | A      |
| 11 | A      |
| 12 | C      |
| 13 | A      |
| 14 | A      |
| 15 | A      |
| 16 | A      |
| 17 | A      |
| 18 | A      |
| 19 | A      |
| 20 | A      |
| 21 | A      |
| 22 | A      |
| 23 | A      |
| 24 | A      |
| 25 | A      |
| 26 | A      |
| 27 | A      |
| 28 | A      |
| 29 | A      |
| 30 | A      |
| 31 | A      |
| 32 | A      |
| 33 | A      |
| 34 | A      |
| 35 | A      |
| 36 | A      |
| 37 | A      |
| 38 | A      |
| 39 | A      |
| 40 | D      |

---

---

## **Python String Manipulation – Hard MCQs (50 Questions)**

---

**1. What is the output of:**  
```python
s = "SEBI2025"
print(s[1:-1:2])
```  
A) `"EI0"`  
B) `"EB22"`  
C) `"E2"`  
D) `"SBI2"`  

**2. Output of:**  
```python
s = "Python"
print(s[::-2])
```  
A) `"nhy"`  
B) `"nhyP"`  
C) `"nht"`  
D) `"nyh"`  

**3. What does the following return?**  
```python
s = "SEBI2025"
print(s.replace("2","X",2))
```  
A) `"SEXBIX05"`  
B) `"SEXBI2025"`  
C) `"SXXBI2025"`  
D) `"SEBXIX25"`  

**4. Output of:**  
```python
s = "SEBI2025"
print(s[::3])
```  
A) `"S22"`  
B) `"S25"`  
C) `"SB2"`  
D) `"S2I"`  

**5. What is returned by:**  
```python
re.findall(r"[A-Za-z]{2}", "SEBI2025")
```  
A) `['SE', 'BI']`  
B) `['SE', 'EB', 'BI']`  
C) `['SE', 'BI', '25']`  
D) `['S', 'E', 'B', 'I']`  

**6. Which slice gives the last 4 characters of `s = "SEBIGradeA"`?**  
A) `s[-4:]`  
B) `s[-5:-1]`  
C) `s[5:9]`  
D) `s[4:8]`  

**7. Output of:**  
```python
"SEBI2025".find("2",2)
```  
A) 4  
B) 1  
C) 2  
D) 3  

**8. Output of:**  
```python
"banana".rindex("a",0,5)
```  
A) 3  
B) 4  
C) 2  
D) 5  

**9. What does:**  
```python
"abc123".isalnum()
```  
return?  
A) True  
B) False  
C) None  
D) Error  

**10. Output of:**  
```python
"Hello World".split(" ",1)
```  
A) `['Hello', 'World']`  
B) `['Hello', 'World', '']`  
C) `['Hello World']`  
D) `['H','ello World']`  

**11. Which regex matches a string that starts with `"SEBI"` and ends with digits?**  
A) `^SEBI\d+$`  
B) `SEBI\d+`  
C) `^SEBI\d*`  
D) `SEBI[0-9]`  

**12. Output of:**  
```python
"SEBI2025".partition("2")
```  
A) `('S', '2', 'EBI2025')`  
B) `('SEBI', '2', '025')`  
C) `('S', '2', 'EBI025')`  
D) `('SEBI', '2', '025')`  

**13. What is the output of:**  
```python
"SEBI2025".swapcase()
```  
A) `"sebi2025"`  
B) `"SEBI2025"`  
C) `"SebI2025"`  
D) `"sEBI2025"`  

**14. Which code checks if all characters are either letters or digits?**  
A) `"SEBI2025".isalnum()`  
B) `"SEBI2025".isalpha()`  
C) `"SEBI2025".isdigit()`  
D) `"SEBI2025".isnumeric()`  

**15. Output of:**  
```python
"python programming".title()
```  
A) `"Python Programming"`  
B) `"Python programming"`  
C) `"PYTHON PROGRAMMING"`  
D) `"PythonProgramming"`  

**16. What does:**  
```python
"abc123xyz".isnumeric()
```  
return?  
A) False  
B) True  
C) Error  
D) None  

**17. Output of:**  
```python
"SEBI2025".ljust(10,"*")
```  
A) `"SEBI2025**"`  
B) `"**SEBI2025"`  
C) `"SEBI2025"`  
D) `"SEBI*2025"`  

**18. Which regex finds overlapping digits in `"1111"`?**  
A) `(?=(\d\d))`  
B) `\d{2}`  
C) `\d+`  
D) `\d*`  

**19. Output of:**  
```python
"Python".zfill(8)
```  
A) `"00Python"`  
B) `"000Python"`  
C) `"Python00"`  
D) `"Python"`  

**20. What does:**  
```python
"SEBI2025".count("2",1,6)
```  
return?  
A) 2  
B) 1  
C) 3  
D) 0  

**21. Which code removes both leading and trailing whitespaces from `s`?**  
A) `s.strip()`  
B) `s.lstrip()`  
C) `s.rstrip()`  
D) `s.trim()`  

**22. Output of:**  
```python
"abc123abc".replace("a","A",2)
```  
A) `"Abc123Abc"`  
B) `"Abc123ABC"`  
C) `"ABC123ABC"`  
D) `"Abc123abc"`  

**23. What is returned by:**  
```python
"Hello World".rpartition(" ")
```  
A) `('Hello', ' ', 'World')`  
B) `('Hello World', '', '')`  
C) `('Hello ', 'W', 'orld')`  
D) `('Hello', '', 'World')`  

**24. Which method returns the index of `"B"` in `"SEBI"` or raises an error if not found?**  
A) `"SEBI".index("B")`  
B) `"SEBI".find("B")`  
C) `"SEBI".rfind("B")`  
D) `"SEBI".search("B")`  

**25. Output of:**  
```python
"abc123".capitalize()
```  
A) `"Abc123"`  
B) `"ABC123"`  
C) `"abc123"`  
D) `"Abc123ABC"`  

**26. Which regex matches 3 to 5 digits exactly?**  
A) `\d{3,5}`  
B) `\d{3,}`  
C) `\d{5}`  
D) `\d+`  

**27. Output of:**  
```python
s = "banana"
print(s[::-1].replace("a","A",2))
```  
A) `"bAnAnA"`  
B) `"bAnanA"`  
C) `"bAnAnA"`  
D) `"bAnanA"`  

**28. Which method checks if all letters in `"Python"` are lowercase?**  
A) `.islower()`  
B) `.isupper()`  
C) `.isalpha()`  
D) `.istitle()`  

**29. Output of:**  
```python
"SEBI2025".expandtabs(4)
```  
A) `"SEBI2025"`  
B) `"SEBI    2025"`  
C) `"SEBI\t2025"`  
D) `"S E B I 2 0 2 5"`  

**30. Which slice reverses a string but keeps only even indexed characters?**  
A) `s[::-2]`  
B) `s[::2]`  
C) `s[::-1]`  
D) `s[1::2]`  

**31. Output of:**  
```python
"Hello World".splitlines()
```  
A) `['Hello World']`  
B) `['Hello','World']`  
C) `['Hello World','']`  
D) `['Hello\nWorld']`  

**32. What does:**  
```python
any(c.isdigit() for c in "abc123")
```  
return?  
A) True  
B) False  
C) None  
D) Error  

**33. Output of:**  
```python
re.findall(r"[A-Z]{2,}","SEBI2025 RBI")
```  
A) `['SEBI','RBI']`  
B) `['SEBI']`  
C) `['RBI']`  
D) `['SE','BI','RB','I']`  

**34. Which expression converts a list of strings to a single comma-separated string?**  
A) `",".join(["SEBI","RBI"])`  
B) `"SEBI","RBI".join(",")`  
C) `",".concat(["SEBI","RBI"])`  
D) `",".append(["SEBI","RBI"])`  

**35. Output of:**  
```python
"abc123abc".translate(str.maketrans("abc","ABC"))
```  
A) `"ABC123ABC"`  
B) `"abc123abc"`  
C) `"AbC123AbC"`  
D) `"ABC123abc"`  

**36. Output of:**  
```python
"Python".removeprefix("Py")
```  
A) `"thon"`  
B) `"Python"`  
C) `"Py"`  
D) `"thonPy"`  

**37. Output of:**  
```python
"Python".removesuffix("on")
```  
A) `"Pyth"`  
B) `"Python"`  
C) `"Py"`  
D) `"Python"`  

**38. What does:**  
```python
s = "SEBI2025"
s[::0]
```  
return?  
A) Error  
B) `"SEBI2025"`  
C) `""`  
D) `None`  

**39. Output of:**  
```python
"Hello World".rjust(15,"-")
```  
A) `"----Hello World"`  
B) `"Hello World----"`  
C) `"Hello World"`  
D) `"---Hello World"`  

**40. Which regex matches any character except digits?**  
A) `\D`  
B) `\d`  
C) `.`  
D) `\w`  

**41. Output of:**  
```python
"abc123".partition("x")
```  
A) `('abc123','','')`  
B) `('a','x','bc123')`  
C) `('abc','x','123')`  
D) `('', 'x','abc123')`  

**42. Which code checks if `"SEBI2025"` contains `"EBI"`?**  
A) `"EBI" in "SEBI2025"`  
B) `"SEBI2025".has("EBI")`  
C) `"SEBI2025".contains("EBI")`  
D) `"SEBI2025".find("EBI")`  

**43. Output of:**  
```python
"abc123".rjust(8,"0")
```  
A) `"00abc123"`  
B) `"000abc123"`  
C) `"abc12300"`  
D) `"abc123"`  

**44. Which method returns the index of `"X"` in `"SEBI"` or -1 if not found?**  
A) `.find("X")`  
B) `.index("X")`  
C) `.rindex("X")`  
D) `.search("X")`  

**45. Output of:**  
```python
"Python".encode().decode()
```  
A) `"Python"`  
B) `b'Python'`  
C) `None`  
D) `Error`  

**46. Which code replaces all vowels with `*` in `"banana"`?**  
A) `re.sub(r"[aeiou]","*","banana")`  
B) `"banana".replace("[aeiou]","*")`  
C) `re.findall(r"[aeiou]","banana")`  
D) `"banana".sub("[aeiou]","*")`  

**47. Output of:**  
```python
s = "Python123"
print(s.isascii())
```  
A) True  
B) False  
C) None  
D) Error  

**48. Output of:**  
```python
"abc123abc".rpartition("123")
```  
A) `('abc', '123', 'abc')`  
B) `('abc123', '', 'abc')`  
C) `('abc', '1', '23abc')`  
D) `('abc','12','3abc')`  

**49. Which slice returns every 3rd character of `"SEBI2025"` starting from index 0?**  
A) `s[::3]`  
B) `s[0::2]`  
C) `s[1::3]`  
D) `s[2::3]`  

**50. Output of:**  
```python
"Python".isascii()
```  
A) True  
B) False  
C) Error  
D) None  

---

### **Answer Key Table (2 Columns)**

| Q# | Answer | Q# | Answer |
|----|--------|----|--------|
| 1  | A      | 26 | A      |
| 2  | A      | 27 | B      |
| 3  | A      | 28 | A      |
| 4  | B      | 29 | A      |
| 5  | A      | 30 | A      |
| 6  | A      | 31 | A      |
| 7  | A      | 32 | A      |
| 8  | A      | 33 | A      |
| 9  | A      | 34 | A      |
| 10 | A      | 35 | A      |
| 11 | A      | 36 | A      |
| 12 | D      | 37 | A      |
| 13 | A      | 38 | A      |
| 14 | A      | 39 | A      |
| 15 | A      | 40 | A      |
| 16 | A      | 41 | A      |
| 17 | A      | 42 | A      |
| 18 | A      | 43 | A      |
| 19 | A      | 44 | A      |
| 20 | A      | 45 | A      |
| 21 | A      | 46 | A      |
| 22 | A      | 47 | A      |
| 23 | A      | 48 | A      |
| 24 | A      | 49 | A      |
| 25 | A      | 50 | A      |

---
